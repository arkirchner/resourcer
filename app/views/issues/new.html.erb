<%= form_with model: @issue, url: project_issues_path(@issue.project) do |f| %>
  <%= f.label :subject %>
  <%= f.text_field :subject %>
  <%= f.label :due_at %>
  <%= f.date_select :due_at %>
  <%= f.submit %>
<% end %>
